{
  "name": "webpack-module-split",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run clean:dist && cp -R @example node_modules/ && webpack-cli --mode production",
    "clean:dist": "rimraf dist/"
  },
  "devDependencies": {
    "rimraf": "^3.0.0",
    "webpack": "^4.43.0",
    "webpack-cli": "^3.3.9"
  },
  "dependencies": {
    "@babel/core": "^7.10.2",
    "@babel/preset-env": "^7.10.2",
    "babel-loader": "^8.1.0"
  }
}
